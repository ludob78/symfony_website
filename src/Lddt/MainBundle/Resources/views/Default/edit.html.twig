{% extends "@LddtMain/n2.html.twig" %}
{% form_theme form 'bootstrap_3_layout.html.twig' %}
{% block n3 %}
    <!-- Template Niveau 3 -->
    <div class="row well well-lg">

            <div class="col-lg-3 col-lg-offset-1">
                <h1>Modification du dessin :{{ draw.title|upper }} </h1>
                    <img width="290" src="{{ asset(draw.pic.webPath) }}" />
                {#{{ dump(asset(draw.pic.webPath)) }}#}
               </div>
            <div class="col-lg-5 col-lg-offset-1">
                <h3>Envoyez votre dessin !</h3>
                <!-- Template Formulaire -->
                {#{{ dump(form) }}#}
             {#   {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}#}
                {% include 'LddtMainBundle:Default/Partials:form_draw.html.twig' %}
                <!-- Fin Template Formulaire -->
                <!-- Si le visiteur n'est pas identifié -->
                <!-- <p>Merci de vous<a href="#"> indentifier</a>  pour poster vos dessins !</p> -->
            </div>

    </div><!-- Fin Template Niveau 3 -->
{% endblock n3 %}